# Инструкция по исправлению синхронизации шаблонов

## ✅ Выполненные изменения

### 1. Исправлен файл `admin/src/data/mockClubs.ts`

**Было:**
```typescript
export const mockTemplates: Template[] = [
  createTemplateDraft({ clubId: mockClubs[0].id, name: 'Анонс забега' }),
  createTemplateDraft({ clubId: mockClubs[0].id, name: 'Результаты' }),
  createTemplateDraft({ clubId: mockClubs[1].id, name: 'Новичкам' }),
]
```

**Стало:**
```typescript
// Mock-шаблоны убраны - используем только данные из API/БД
// Это обеспечивает соответствие с fallback-шаблонами основного приложения
export const mockTemplates: Template[] = []
```

### 2. Пересобрана админка

Админка успешно пересобрана с новыми настройками:
```bash
cd admin && npm run build
✓ built in 3.17s
```

### 3. Создана локальная диагностика

Создан скрипт `check_template_sync_local.py` для проверки синхронизации без подключения к БД.

## 🎯 Что нужно сделать на продакшене

### Шаг 1: Синхронизировать fallback-шаблоны с БД

На сервере с доступом к базе данных выполните:

```bash
# Убедитесь, что DATABASE_URL установлена
echo $DATABASE_URL

# Синхронизируйте fallback-шаблоны
python3 sync_fallback_templates.py
```

Скрипт:
- ✅ Создаст 6 fallback-шаблонов в БД (по 3 для каждого клуба)
- ✅ Установит статус `published` для всех шаблонов
- ✅ Покажет детальную статистику

### Шаг 2: Проверить результат

```bash
# Проверить шаблоны в БД
python3 check_templates.py

# Или сравнить fallback с БД
python3 compare_templates.py
```

### Шаг 3: Проверить через веб-интерфейс

**Вариант A: Через основное приложение**
1. Откройте `/route/`
2. Подключитесь к Strava
3. Выберите клуб (NOT IN PARIS или HEDONISM)
4. Выберите тренировку
5. Посмотрите на список шаблонов

**Вариант B: Через админку**
1. Откройте `/route/admin`
2. Войдите (admin / ваш_пароль)
3. Выберите клуб
4. Посмотрите на список шаблонов

**Вариант C: Через страницу синхронизации**
1. Откройте `/route/admin/sync`
2. Нажмите "Проверить шаблоны"
3. Если нужно - нажмите "Запустить синхронизацию"

## 📊 Ожидаемые шаблоны после синхронизации

### NOT IN PARIS (club_id: `not-in-paris`)
| ID             | Название       | Статус    | Описание                                              |
|----------------|----------------|-----------|-------------------------------------------------------|
| nip-classic    | Classic Route  | published | Standard overlay with club logo and clean typography  |
| nip-mono       | Mono Cut       | published | High-contrast monochrome look for bold storytelling   |
| nip-gradient   | Sunset Fade    | published | Gradient background with bright typography accents    |

### HEDONISM (club_id: `hedonism`)
| ID               | Название       | Статус    | Описание                                                |
|------------------|----------------|-----------|---------------------------------------------------------|
| hedonism-classic | Hedonism Core  | published | Signature hedonism palette with vivid logo lockup       |
| hedonism-night   | Night Drive    | published | Dark mode composition with neon typography highlights   |
| hedonism-mono    | Mono Pulse     | published | Monochrome variant for poster-ready storytelling        |

## 🔍 Проверка синхронизации

После выполнения всех шагов проверьте:

- [ ] В БД есть 6 шаблонов (по 3 для каждого клуба)
- [ ] Все шаблоны имеют статус `published`
- [ ] Основное приложение показывает шаблоны из БД (не fallback)
- [ ] Админка показывает те же шаблоны
- [ ] ID и названия шаблонов совпадают между приложениями

### Как проверить, что основное приложение использует БД?

Откройте консоль браузера (F12) и посмотрите на логи:

**Если используется БД:**
```
✅ Loaded 3 templates from API for club: not-in-paris
```

**Если используется fallback:**
```
⚠️ Failed to load templates from API, using fallback
```

## 🐛 Устранение проблем

### Проблема: Основное приложение показывает fallback-шаблоны

**Причина:** API возвращает пустой массив или ошибку

**Решение:**
1. Проверьте, есть ли шаблоны в БД: `python3 check_templates.py`
2. Проверьте, что шаблоны имеют статус `published`
3. Запустите синхронизацию: `python3 sync_fallback_templates.py`
4. Проверьте API напрямую:
   ```bash
   curl "https://your-domain/api/templates?clubId=not-in-paris"
   ```

### Проблема: Админка показывает пустой список шаблонов

**Причина:** БД пустая или API не работает

**Решение:**
1. Проверьте логи сервера
2. Проверьте DATABASE_URL
3. Запустите синхронизацию: `python3 sync_fallback_templates.py`
4. Обновите страницу админки (Ctrl+Shift+R)

### Проблема: Шаблоны есть, но со статусом "draft"

**Причина:** Синхронизация не была запущена

**Решение:**
1. Запустите: `python3 sync_fallback_templates.py`
2. Или через веб-интерфейс: `/route/admin/sync` → "Запустить синхронизацию"

## 📚 Дополнительные материалы

- `ДИАГНОСТИКА_СИНХРОНИЗАЦИИ.md` - детальная диагностика проблемы
- `docs/TEMPLATE_SYNC_FIX.md` - подробное описание решения
- `docs/TEMPLATE_SYNC_WEB.md` - веб-интерфейс синхронизации
- `docs/SYNC_CHANGES.md` - история изменений
- `sync_fallback_templates.py` - скрипт синхронизации
- `check_templates.py` - скрипт проверки БД
- `compare_templates.py` - скрипт сравнения
- `check_template_sync_local.py` - локальная проверка (без БД)

## 💡 Важные заметки

1. **База данных - единственный источник истины**
   - Fallback-шаблоны в коде используются только когда БД недоступна
   - Mock-данные в админке больше не используются

2. **Автосохранение в админке**
   - Админка автоматически сохраняет изменения каждые 30 секунд
   - Все изменения сразу попадают в БД

3. **Статусы шаблонов**
   - `published` - виден везде (основное приложение + админка)
   - `draft` - виден только в админке
   - `archived` - виден только в админке
   - `deleted` - не виден нигде

4. **Кеширование**
   - После изменений в БД обновите страницы (Ctrl+Shift+R)
   - React Query кеширует данные на 5 минут

## ✨ Результат

После выполнения всех шагов:

✅ Админка и основное приложение показывают **одинаковые** шаблоны  
✅ Шаблоны загружаются из **базы данных**  
✅ Все шаблоны имеют статус **published**  
✅ ID и названия шаблонов **соответствуют** друг другу  
✅ Система работает **корректно** и **предсказуемо**  

---

*Последнее обновление: 2024-11-14*

