# 🔧 Быстрое исправление несоответствия шаблонов

## Проблема
- Основное приложение показывает: **Classic Route, Mono Cut, Sunset Fade**
- Админка показывает: **1 published и 2 draft**
- Шаблоны не совпадают ❌

## ⚡ Быстрое решение (5 минут)

### На сервере Railway:

```bash
# 1. Зайдите в Dashboard Railway → Your Project → Terminal

# 2. Запустите скрипт синхронизации:
python3 sync_fallback_templates.py

# 3. Дождитесь завершения (появится ✅)

# 4. Обновите основное приложение и админку (Ctrl+Shift+R)
```

### Локально (для разработки):

```bash
# 1. Установите DATABASE_URL из Railway
export DATABASE_URL="postgresql://postgres:ваш_пароль@регион.railway.app:порт/railway"

# 2. Запустите скрипт:
python3 sync_fallback_templates.py

# 3. Проверьте результат:
python3 check_templates.py
```

## Что делает скрипт?

1. ✅ Создает в БД фолбэк-шаблоны:
   - Classic Route
   - Mono Cut  
   - Sunset Fade

2. ✅ Устанавливает статус `published` для всех

3. ✅ Синхронизирует ID между кодом и БД

4. ✅ Показывает итоговую статистику

## После запуска

**Основное приложение** `/route/`:
- Загрузит шаблоны из БД
- Покажет: Classic Route, Mono Cut, Sunset Fade
- Статус: Published ✅

**Админка** `/route/admin`:
- Покажет те же 3 шаблона
- Все в статусе Published ✅
- Можно редактировать и создавать новые

## 🆘 Если что-то пошло не так

### DATABASE_URL не установлена

```bash
# На Railway проверьте переменные окружения:
railway variables

# Должна быть DATABASE_URL (создается автоматически при добавлении PostgreSQL)
```

### Ошибка подключения к БД

```bash
# Проверьте, что PostgreSQL добавлен в проект Railway
# Dashboard → Add Service → Database → PostgreSQL
```

### Шаблоны не появились

```bash
# 1. Проверьте, что скрипт завершился успешно (✅)
# 2. Проверьте БД:
python3 check_templates.py

# 3. Очистите кеш браузера (Ctrl+Shift+R)
# 4. Проверьте консоль браузера (F12) на ошибки
```

## 📚 Подробная документация

См. `/docs/TEMPLATE_SYNC_FIX.md` для:
- Подробного объяснения проблемы
- Альтернативных решений
- Архитектуры системы
- Рекомендаций на будущее



