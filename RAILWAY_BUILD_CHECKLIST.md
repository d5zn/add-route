# Railway Build Checklist

## ✅ Перед деплоем проверьте:

### 1. Переменные окружения в Railway Dashboard

Убедитесь, что все переменные установлены в Railway → Variables:

```bash
# Обязательные
DATABASE_URL=postgresql://user:password@host:port/database?schema=public
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# Админ-панель
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your_secure_password
ADMIN_SESSION_SECRET=your_secure_session_secret

# Strava OAuth
STRAVA_CLIENT_ID=your_strava_client_id
STRAVA_CLIENT_SECRET=your_strava_client_secret
STRAVA_REDIRECT_URI=https://your-app.railway.app/api/strava/callback

# Next.js
NEXT_PUBLIC_APP_URL=https://your-app.railway.app
```

### 2. DATABASE_URL должен быть установлен ДО билда

**Важно:** Railway автоматически устанавливает DATABASE_URL при подключении PostgreSQL базы данных. Убедитесь, что:
- PostgreSQL сервис подключен к вашему проекту
- DATABASE_URL виден в Variables

### 3. Проверка билда

После установки переменных:
1. Запустите новый деплой
2. Проверьте логи билда в Railway Dashboard
3. Убедитесь, что билд проходит успешно

## 🔧 Если билд все еще падает

### Проверьте логи

В Railway Dashboard → Deployments → View Logs найдите конкретную ошибку.

### Возможные проблемы:

1. **"Can't resolve module"** - проблема с зависимостями
   - Решение: Проверьте `package.json` и убедитесь, что все зависимости установлены

2. **"Prisma Client not generated"** - проблема с Prisma
   - Решение: Убедитесь, что `npx prisma generate` выполняется успешно
   - Проверьте, что `prisma/schema.prisma` корректен

3. **"DATABASE_URL is required"** - отсутствует DATABASE_URL
   - Решение: Подключите PostgreSQL сервис или установите DATABASE_URL вручную

4. **"Module not found"** - проблема с путями импорта
   - Решение: Убедитесь, что `tsconfig.json` и `jsconfig.json` настроены правильно

## 📝 Изменения в коде

### lib/db.ts
- Улучшена обработка отсутствия DATABASE_URL во время билда
- Добавлена ленивая инициализация Prisma клиента
- Добавлены проверки для build-time окружения

### nixpacks.toml
- Prisma generate явно вызывается в фазе build
- Добавлена обработка ошибок для Prisma generate

## 🚀 После успешного билда

1. Проверьте, что приложение запускается
2. Проверьте healthcheck endpoint (`/`)
3. Проверьте админ-панель (`/admin/login`)
4. Проверьте API endpoints

## 📞 Поддержка

Если проблема сохраняется:
1. Проверьте полные логи билда
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что локальный билд работает (`npm run build`)
4. Создайте issue с полными логами ошибки

